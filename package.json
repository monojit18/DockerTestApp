{
  "name": "dockertestapp",
  "version": "1.0.1",
  "description": "Test Docker",
  "main": "dockerserver.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "mkdir -p logs",
    "install": "npm install --save-dev express body-parser request",
    "start": "node dockerserver.js 1> logs/dockerserver.log 2>& 1"
  },
  "keywords": [
    "docker"
  ],
  "author": "Monojit Datta",
  "license": "MIT",
  "devDependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "request": "^2.88.0"
  }
}
